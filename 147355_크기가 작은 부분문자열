def solution(t, p):
    list = []
    n = len(p)
    
    answer = 0
    
    for i in range(len(t) - n + 1):
        if t[i] != 0:
            list.append(t[i:i+n])
    
    for j in list:
        if int(j) <= int(p):
            answer += 1

    return answer
